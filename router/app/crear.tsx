import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Crear = () => {

  return (
    <SafeAreaView>
      <View>
        <Text>Crear Screen</Text>
      </View>
    </SafeAreaView>
  );
}

export default Crear;